version: 2

sources:
  - name: flights_raw
    tables:
      - name: lnd_flights
        meta:
          external_location: "s3://flights-data-lake/bronze/insert_date={{ var('execution_date') }}/*.parquet"
